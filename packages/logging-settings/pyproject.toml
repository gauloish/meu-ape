[project]
name = "logging-settings"
version = "0.1.0"
description = "Pacote com configurações personalizadas de logging para o projeto meu-ape."
readme = "README.md"
authors = [
    { name = "gauloish", email = "125079293+gauloish@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
logging-settings = "logging_settings:main"

[build-system]
requires = ["uv_build>=0.11.18,<0.12.0"]
build-backend = "uv_build"
